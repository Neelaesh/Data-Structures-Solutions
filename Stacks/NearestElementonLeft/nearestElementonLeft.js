console.log("nearestElementonLeft");

function nearestElementonLeft(arr) {
    // Create an empty Stack
    var stack = []
    for(let i = 0; i<arr.length; i++) {
        
    }
    
}

let arr = [ 1, 3, 0, 2, 5 ]
console.log(nearestElementonLeft(arr))